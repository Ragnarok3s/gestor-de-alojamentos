<div class="bo-page bo-page--wide">
  <a class="text-slate-600 underline" href="/admin">&larr; Backoffice</a>
  <header class="mb-6">
    <h1 class="text-2xl font-semibold">Gestão de propriedades</h1>
    <p class="text-sm text-slate-600 max-w-2xl">
      Consulta rapidamente todas as propriedades configuradas, filtra por estado e ordena os resultados. Usa as ações para
      editar detalhes, duplicar configurações ou eliminar alojamentos que já não sejam necessários.
    </p>
  </header>

  <section class="card p-4">
    <div class="flex flex-col gap-2 mb-4 md:flex-row md:items-center md:justify-between">
      <div>
        <h2 class="text-lg font-semibold text-slate-800">Listagem de propriedades</h2>
        <p class="text-sm text-slate-500">Encontradas <strong><%= totalCount %></strong> propriedades.</p>
      </div>
      <a class="btn btn-primary" href="/admin">Criar nova propriedade</a>
    </div>
    <%- renderTable(table) %>
  </section>
</div>
